[module/my_wan]
type = custom/script

interval = 10.0

exec = myip

format = "WAN <label>"
format-padding = 1
format-label = "WAN "
format-label-foreground = ${unit.wan}
format-foreground = ${unit.wan}
format-underline = ${unit.wan}
label-foreground = ${theme.white}